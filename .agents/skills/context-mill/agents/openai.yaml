interface:
  display_name: "Context Mill"
  short_description: "Repo-specific patterns and workflows for context-mill"
  default_prompt: "Use the context-mill repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true