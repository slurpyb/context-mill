{
  "name": "context-mill",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Context Mill: assembling and packaging context for AI agents and LLMs",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/dev-server.js",
    "test:plugins": "vitest run scripts/plugins/tests",
    "test:plugins:watch": "vitest scripts/plugins/tests",
    "test:skills": "vitest run scripts/lib/tests",
    "test:skills:watch": "vitest scripts/lib/tests",
    "test": "vitest run scripts/plugins/tests scripts/lib/tests"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "chokidar": "^4.0.3",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
